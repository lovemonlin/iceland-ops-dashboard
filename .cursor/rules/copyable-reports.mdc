---
description: 完成報告必須是單一可一鍵複製區塊
alwaysApply: true
---

# 完成報告：一鍵複製

禎禎每一次都要能一鍵複製整份報告。不要再拆成聊天散落的編號清單。

- 只要使用者要求「回報 / 完成後回報 / 輸出報告」，最終回覆的**主體**必須是**一個** Markdown fenced code block（` ```text ` 包住全文）。
- 該區塊內含完整編號答案，可直接全選複製。區塊外最多一句說明，不要把報告內容寫在區塊外。
- 不要用必須逐段挑的表格、必須點很多處的散落標題，來替代這一份可複製全文。
- 之後每一次 phase wrap-up、git 收尾、驗收報告都遵守。
